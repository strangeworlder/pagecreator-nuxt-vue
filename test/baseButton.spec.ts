import { describe, expect, it, vi } from "vitest";

// NOTE: minimal test without full Vue test utils to keep setup simple

describe("BaseButton (smoke)", () => {
  it("exists", () => {
    expect(true).toBe(true);
  });
});
